#!/usr/bin/env bash
set -e
set -u
set -o pipefail

if [[ $# -gt 0 ]]; then
  echo "Error: 'gempty' takes no arguments." >&2
  echo "Usage: gempty" >&2
  exit 1
fi

echo "Permanently emptying the entire trash..." >&2
exec gtrash find --rm
